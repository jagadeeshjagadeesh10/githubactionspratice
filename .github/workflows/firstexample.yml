name: my first flow 

on: push 

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps: 
            - name: welcome message
              run: echo "my first github trigger"
            
            - name: List files
              run: list 
            
            - name: Read File
              run: cat Readme.md 
            
            