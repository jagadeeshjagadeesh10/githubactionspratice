name: my first flow 

on: push 

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout epo
              uses: actions/checkout@v4 

            - name: List and Read file
              run: 
                echo "my first github trigger"
                ls -ltra
                cat README.md
            
            #- name: List files
            #  run: ls 
            
            #- name: Read File
            # run: cat README.md             