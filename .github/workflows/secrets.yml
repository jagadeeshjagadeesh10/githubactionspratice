name: CHECKING SECRETS

on:
  push

jobs:
  getting_started:
    runs-on: ubuntu-latest
    steps: 
      - name: "Checking secrets value"
        run: |
          echo "Secret value: ${{ secrets.KEY }}"
          ${{ secrets.KEY }}
      
