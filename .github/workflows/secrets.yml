name: CHECKING SECRETS

on:
  workflow_dispatch

jobs:
  getting_started:
    runs-on: ubuntu-latest
    steps: 
      - name: "Checking secrets value"
        run: |
          echo "Secret value: ${{ secrets.KEY }}"
          ${{ secrets.KEY }}
        env:
          ACTIONS_STEP_DEBUG: true

